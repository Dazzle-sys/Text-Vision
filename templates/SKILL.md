---
name: text-vision
description: 当任务涉及图片、截图、屏幕、界面、UI、OCR、识别、验证码、程序窗口时使用,把图片或窗口画面转为文字描述。触发词:图片 / 截图 / 屏幕 / 界面 / 程序窗口 / UI / OCR / 识别。
---

# 视觉:把图片转成文字

本机文本模型(DeepSeek 等)看不懂图片。按需调用 text-vision 的工具:

1. **用户粘贴/拖入的图片** → 宿主工具已把图片存为本地文件,消息里通常带路径或文件名线索。**不要回复"我不支持看图",不要向用户索要路径。** 自行定位:优先用消息里的路径;只有文件名时,搜索临时/项目目录里最近创建的图片;定位后调 `describe_image(path)`;验证码/报错/文档截图优先 `ocr_image(path)`
2. **本地图片文件** → `describe_image(path, focus?)`;若是验证码/报错/文档截图,优先 `ocr_image(path)`
3. **主动截图看运行中的程序/界面** → 任务涉及某个正在运行的程序/窗口(用户问"这个程序在干嘛/什么状态",或需要看界面布局、验证操作结果、检查报错弹窗)时**主动截图,不要等用户截或贴图**。流程:`list_windows()` 拿窗口清单 → 按窗口 ID/进程名/标题定 target → `screen_capture(target=…)`;界面是动态的,**关键操作后重新截图确认**。`clientArea=true`(仅 Windows)截客户区去边框标题栏。护栏:截图会上传第三方视觉 API,目标窗口可能含密码/账号/聊天记录/证件等敏感画面时**先问用户确认再截**;找不到匹配窗口会明确报错,先确认目标进程在运行,不要瞎编界面内容
4. 拿到文字后,基于它继续分析,不要尝试读图片二进制

若 PreToolUse hook 已生效,读图会自动被转成描述,本技能作为双保险。
